apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperpub.fullname" . }}
  labels:
    {{- include "paperpub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paperpub.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "paperpub.fullname" . }}-secret
  labels:
    {{- include "paperpub.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secret }}
  {{- if $val }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
  {{- end }}

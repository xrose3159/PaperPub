apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "paperpub.fullname" . }}-config
  labels:
    {{- include "paperpub.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{- if $val }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  {{- end }}
